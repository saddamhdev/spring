ঠিক আছে 👍
এবার **Spring Bean Life Cycle** একদম **step-by-step + real example** দিয়ে বুঝাই।
এইটা বুঝলে Spring-এর ৫০% clear হয়ে যায়।

---

# 🌱 Spring Bean Life Cycle (With Real Example)

![Image](https://howtodoinjava.com/wp-content/uploads/Spring-bean-life-cycle.png)

![Image](https://bootcamptoprod.com/wp-content/uploads/2023/09/Spring-Bean-Life-cycle-Flow.png)

![Image](https://i.sstatic.net/kpcdR.png)

---

## 🧠 Bean Life Cycle মানে কী?

> **Spring container একটি Bean তৈরি করার পর থেকে destroy করা পর্যন্ত যেসব ধাপ পার হয়—সেটাই Bean Life Cycle**

---

## 🪜 Bean Life Cycle — Big Picture

```
1. Scan
2. Bean Definition
3. Instantiate
4. Dependency Injection
5. Initialization
6. Ready to Use
7. Destroy
```

এখন একে একে **বাস্তব কোড দিয়ে** দেখি 👇

---

## 🧩 Example Bean

```java
@Component
public class UserService {

    public UserService() {
        System.out.println("1️⃣ Constructor: UserService object created");
    }

    @Autowired
    private EmailService emailService;

    @PostConstruct
    public void init() {
        System.out.println("5️⃣ @PostConstruct: Bean initialized");
    }

    @PreDestroy
    public void destroy() {
        System.out.println("7️⃣ @PreDestroy: Bean destroyed");
    }
}
```

```java
@Component
class EmailService {
    public EmailService() {
        System.out.println("EmailService created");
    }
}
```

---

# 🔍 Step-by-Step Life Cycle

---

## ✅ Step 1: Component Scan

Spring app চালু হলে—

```java
@SpringBootApplication
public class App {
    public static void main(String[] args) {
        SpringApplication.run(App.class, args);
    }
}
```

Spring:

* `@Component` খোঁজে
* `UserService` detect করে

📌 **এখনো object তৈরি হয়নি**

---

## ✅ Step 2: Bean Definition তৈরি

Spring মনে মনে একটা plan বানায়:

```
Bean Name: userService
Class: UserService
Scope: singleton
Dependencies: EmailService
```

📌 **এটা শুধু metadata**

---

## ✅ Step 3: Instantiation (Object তৈরি)

Spring constructor call করে:

```text
1️⃣ Constructor: UserService object created
```

মানে:

```java
new UserService();
```

---

## ✅ Step 4: Dependency Injection

Spring দেখে:

```java
@Autowired
EmailService emailService;
```

তাই:

1. `EmailService` বানায়
2. `UserService` এর ভিতরে inject করে

---

## ✅ Step 5: Initialization

এখন Bean fully ready হওয়ার আগে—

```java
@PostConstruct
public void init() {}
```

Output:

```text
5️⃣ @PostConstruct: Bean initialized
```

📌 এখানেই:

* DB connection
* Cache preload
* Validation

---

## ✅ Step 6: Ready to Use

এখন Bean ব্যবহারযোগ্য ✅

```java
@Autowired
UserService userService;
```

Spring container থেকে দেয়
❌ নতুন বানায় না

---

## ✅ Step 7: Destroy (Application বন্ধ হলে)

App বন্ধ হলে (Ctrl + C):

```text
7️⃣ @PreDestroy: Bean destroyed
```

📌 এখানে:

* Connection close
* Thread stop
* Cleanup

---

## 🔁 Console Output (Realistic)

```
EmailService created
1️⃣ Constructor: UserService object created
5️⃣ @PostConstruct: Bean initialized
...
7️⃣ @PreDestroy: Bean destroyed
```

---

## 🧠 Life Cycle এক লাইনে মনে রাখার কৌশল

> **Create → Inject → Init → Use → Destroy**

---

## 🧩 Alternative Life Cycle Hooks (Advance)

### Using Interfaces

```java
public class A implements InitializingBean, DisposableBean {

    public void afterPropertiesSet() {
        // init
    }

    public void destroy() {
        // cleanup
    }
}
```

### Using @Bean

```java
@Bean(initMethod = "start", destroyMethod = "stop")
public Engine engine() {
    return new Engine();
}
```

---

## ⚠️ Important Notes

| বিষয়            | সত্য               |
| --------------- | ------------------ |
| Constructor     | object বানায়       |
| @Autowired      | dependency inject  |
| @PostConstruct  | init logic         |
| @PreDestroy     | cleanup            |
| Prototype scope | destroy auto হয় না |

---

## 🎯 Interview Ready Answer

> **Spring Bean Life Cycle হলো—component scan থেকে শুরু করে bean creation, dependency injection, initialization, usage এবং application shutdown এ destruction পর্যন্ত সম্পূর্ণ প্রক্রিয়া।**

---

## 🧠 Final Summary

* Bean বানায় → Spring
* Control রাখে → Container
* সময়মতো call করে → lifecycle methods

---

চাও তো next এ দেখাতে পারি:

* 🔥 Prototype vs Singleton lifecycle difference
* 🔥 BeanPostProcessor কী করে
* 🔥 Debug করে lifecycle দেখা
* 🔥 MCQ + Interview questions

বললেই পরের ধাপে যাবো 👍
