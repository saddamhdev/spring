খুব ভালো প্রশ্ন 👍
**এখন তুমি আসল core জায়গায় চলে এসেছো।**
চল একদম পরিষ্কার করে বলি—

---

## ❓ **Spring Container কী দেখে বুঝে — এই class টা manage করতে হবে?**

Spring **নিজে নিজে সব class manage করে না**।
সে কিছু **specific signal (চিহ্ন)** খোঁজে।

---

## 🧠 Spring Container যেসব জিনিস দেখে

### ✅ 1️⃣ **Annotation দেখে (সবচেয়ে গুরুত্বপূর্ণ)**

Spring এই annotation গুলো দেখলে বলে:

> “এই class-টা আমার Bean হবে”

### 🔹 Core annotations

```java
@Component
@Service
@Repository
@Controller
@RestController
```

👉 এগুলোর যেকোনো একটা থাকলেই
➡️ **Spring manage করবে**

---

### ✅ 2️⃣ **Component Scan এর ভিতরে আছে কিনা**

Spring শুধু ওই package গুলো scan করে যেগুলোকে বলা হয়েছে।

```java
@SpringBootApplication
```

এই annotation এর ভিতরে আছে:

```java
@ComponentScan
```

মানে:

* main class যেই package এ আছে
* তার নিচের সব package scan হবে

📌 **এই জায়গার বাইরে থাকলে → Spring দেখবেই না**

---

### ✅ 3️⃣ **`@Configuration + @Bean` method**

```java
@Configuration
class AppConfig {

    @Bean
    public UserService userService() {
        return new UserService();
    }
}
```

এখানে:

* class এ `@Component` নেই
* তবুও Bean হচ্ছে

👉 কারণ Spring `@Bean` দেখেছে

---

### ✅ 4️⃣ **Auto-configuration (Spring Boot special)**

Spring Boot অনেক Bean নিজেই বানায়:

* DataSource
* EntityManager
* TransactionManager
* RedisTemplate

👉 এগুলো আমরা লিখিনি, তবুও Bean

---

## ❌ Spring কী দেখে না?

এইগুলো থাকলে **Bean হবে না** ❌

```java
public class HelperUtil {}
```

* Annotation নেই
* `@Bean` নেই

➡️ Spring ignore করবে

---

## 🧩 সব signal একসাথে (Mind map)

Spring container দেখে:

✔ Annotation আছে?
✔ Scan এর ভিতরে?
✔ `@Bean` method আছে?
✔ Auto-config provided?

👉 কোনো একটা YES হলেই → **Bean**

---

## 🔁 Very Important Shortcut (মাথায় রাখো)

> **Spring only manages what it knows about**

জানার উপায়:

* Annotation
* Configuration
* Auto-config

---

## 🎯 এক লাইনে উত্তর (Exam / Interview ready)

> **Spring Container কোনো class কে manage করে যখন সে `@Component`-type annotation, `@Bean` definition, অথবা auto-configuration এর মাধ্যমে ওই class সম্পর্কে জানে।**

---

## চাইলে next step:

* 🔹 `@Component` ভিতরে কী আছে
* 🔹 `@Service` vs `@Component`
* 🔹 Package ভুল হলে Bean কেন create হয় না
* 🔹 Debug করে Bean creation দেখা

বললেই পরের ধাপে যাবো 👌
