চল এখন এগুলো **কোথায়, কেন, কখন ব্যবহার করো** — একদম পরিষ্কারভাবে বুঝাই।
এটা Spring বুঝতে গেলে **MUST-KNOW** অংশ। 👇

---

# 🌱 Spring Stereotype Annotations — Full Explanation

![Image](https://miro.medium.com/v2/resize%3Afit%3A1200/1%2Ap4JFrO5xD4jGehzWqfHd8w.jpeg)

![Image](https://miro.medium.com/1%2Ah0LDfAhcyyeJwMw1ZAGliQ.png)

![Image](https://www.logicbig.com/tutorials/spring-framework/spring-core/javaconfig-with-componnet-scan/images/scan.png)

---

## 🔰 আগে এক লাইনের Truth

> **এই annotation গুলো Spring-কে signal দেয় — “এই class টা তুমি manage করো (Bean বানাও)”**

---

# 1️⃣ `@Component` — Base / Generic

### 🔹 কী?

সব stereotype annotation-এর **root / parent**।

```java
@Component
public class EmailUtil {}
```

### 🔹 কেন ব্যবহার করি?

* Helper
* Utility
* Validator
* Mapper
* Non-business class

### 🔹 কোথায়?

* যেসব class Service/Repo/Controller না
* কিন্তু Spring-managed দরকার

### 🔹 মনে রাখো

> `@Service`, `@Repository`, `@Controller` → সবই internally `@Component`

---

# 2️⃣ `@Service` — Business Logic Layer

```java
@Service
public class UserService {}
```

### 🔹 কোথায়?

* Business logic
* Rules
* Calculation
* Transaction boundary

### 🔹 কেন?

* Code readability
* Clear architecture
* AOP (Transaction, Security) apply করা সহজ

### 🔹 Extra benefit

* `@Transactional` সাধারণত এখানেই

---

# 3️⃣ `@Repository` — Data Access Layer

```java
@Repository
public class UserRepository {}
```

### 🔹 কোথায়?

* Database access
* DAO / Repository class

### 🔹 কেন?

* Spring **exception translation** করে
  (SQL → DataAccessException)

### 🔹 Special feature

```java
SQLException → DataAccessException
```

👉 `@Component` এ এটা হয় না

---

# 4️⃣ `@Controller` — Web Layer (MVC)

```java
@Controller
public class UserController {}
```

### 🔹 কোথায়?

* Web request handle
* HTML / JSP / Thymeleaf return

### 🔹 কেন?

* View render করার জন্য

```java
@GetMapping("/home")
public String home() {
    return "index"; // HTML
}
```

---

# 5️⃣ `@RestController` — REST API Layer

```java
@RestController
public class UserRestController {}
```

### 🔹 কোথায়?

* REST API
* JSON / XML response

### 🔹 কেন?

* `@Controller + @ResponseBody` shortcut

```java
@GetMapping("/users")
public List<User> users() {
    return service.getAll();
}
```

👉 Response = JSON

---

## 🔁 `@Controller` vs `@RestController`

| বিষয়          | @Controller | @RestController |
| ------------- | ----------- | --------------- |
| Output        | View (HTML) | JSON            |
| @ResponseBody | Needed      | Auto            |
| Use case      | MVC         | REST API        |

---

# 6️⃣ `@ComponentScan` — Bean Discovery

### 🔹 কী?

Spring কে বলে:

> “এই package গুলো scan করো”

```java
@ComponentScan("com.app")
```

### 🔹 Spring Boot এ?

```java
@SpringBootApplication
```

এর ভিতরে already আছে:

```java
@ComponentScan
```

👉 তাই আলাদা দিতে হয় না (৯৫% ক্ষেত্রে)

---

## ⚠️ Common Mistake

```java
package com.other;

@Service
public class TestService {}
```

Main class:

```java
package com.app;
```

❌ Bean হবে না
➡️ কারণ scan এর বাইরে

---

## 🧩 Complete Layered Architecture

```
Controller
   ↓
Service
   ↓
Repository
   ↓
Database
```

| Layer      | Annotation                    |
| ---------- | ----------------------------- |
| Controller | @Controller / @RestController |
| Service    | @Service                      |
| Repository | @Repository                   |
| Helper     | @Component                    |

---

## 🧠 Interview Golden Lines

* **Why @Service?**
  → Business logic marker + AOP clarity

* **Why @Repository?**
  → Exception translation

* **@Component vs @Service?**
  → Semantic difference, same base

* **Why @ComponentScan?**
  → Bean discovery control

---

## 🎯 One-line summary (মুখস্থ করার মতো)

> `@Component` = generic
> `@Service` = business logic
> `@Repository` = database access
> `@Controller` = web MVC
> `@RestController` = REST API
> `@ComponentScan` = Bean খোঁজার জায়গা

---

যদি চাও, পরের ধাপে আমি দেখাতে পারি:

* 🔥 Real project package structure
* 🔥 Annotation ভুল হলে কী error হয়
* 🔥 Custom stereotype annotation
* 🔥 Debug করে component scan দেখা

বললেই শুরু করি 👍
